name: Release Note on PR Merge

on:
  pull_request:
    types: [closed]
    branches: [main]

  workflow_dispatch:
    inputs:
      run_name:
        description: "Enter a run name for this run"
        required: false

jobs:
  release-note:
    if: github.event.pull_request.merged == true || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    env:
      JIRA_API_EMAIL: ${{ secrets.JIRA_API_EMAIL }}
      JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
      JIRA_BASE_URL: https://buyandship.atlassian.net

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Determine run name
        id: set_run_name
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            NAME="${{ github.event.inputs.run_name }}"
            if [[ -z "$NAME" ]]; then
              NAME="Manual run - ${{ github.sha }}"
            fi
          else
            NAME="${{ github.event.pull_request.title }}"
          fi
          echo "Run name: $NAME"
          echo "run_name=$NAME" >> $GITHUB_OUTPUT

      - name: Format release note
        id: format_note
        run: |
          HASH=${{ github.event.pull_request.merge_commit_sha || github.sha }}
          DATE=$(date "+%Y/%m/%d %H:%M")
          TITLE="${{ steps.set_run_name.outputs.run_name }}"

          # 抓出所有 BC-XXXX
          TICKETS=$(echo "$TITLE" | grep -oE 'BC-[0-9]+')
          JIRA_LINKS=""

          for TICKET in $TICKETS; do
            SUMMARY=$(curl -s -u "$JIRA_API_EMAIL:$JIRA_API_TOKEN" \
              -H "Accept: application/json" \
              "$JIRA_BASE_URL/rest/api/2/issue/$TICKET" | jq -r '.fields.summary')

            LINK="[$TICKET] $SUMMARY ($JIRA_BASE_URL/browse/$TICKET)"
            JIRA_LINKS+="$LINK\n"
          done

          if [ -z "$JIRA_LINKS" ]; then
            JIRA_LINKS="No ticket found"
          fi

          NOTE="🚀 *Release Deployed*\n"
          NOTE+="• Repo: \`${{ github.repository }}\`\n"
          NOTE+="• Hash: \`${HASH:0:7}\`\n"
          NOTE+="• Date: \`${DATE}\`\n"
          NOTE+="• Title: ${TITLE}\n"
          NOTE+="• Ticket(s):\n$JIRA_LINKS"
          NOTE+="• View Commit: https://github.com/${{ github.repository }}/commit/${HASH}"

          echo "note=$NOTE" >> $GITHUB_OUTPUT

      - name: Send to Slack
        uses: slackapi/slack-github-action@v1.25.0
        with:
          payload: |
            {
              "text": "${{ steps.format_note.outputs.note }}"
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
